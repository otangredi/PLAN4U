<body id="guest">
  <div class="container-fluid row justify-content-center">
      <div class="mt-3 mb-3" id=<%= @event.e_vite.template_id %> >
        <%= render "#{@event.e_vite.template_id}" %>
      </div>
      <div class="d-flex justify-content-center">
        <%= link_to "RSVP", find_rsvp_path(@event), class: "btn btn-4u" %>
      </div>
  </div>
</body>
