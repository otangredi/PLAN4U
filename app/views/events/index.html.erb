<div class="container mb-3">
  <div class="row justify-content-between m-1">
    <div class="banner text-center col-7">
      <h1><%= current_user.first_name %> & <%= @event.partner_name %></h1>
      <h1><%= @event.date.strftime("%B %e, %Y") %></h1>
      <h1>Only <%= (@event.date - Date.today).to_i %> days to go!</h1>
    </div>

    <div class="plannig_check col-5 bg-white ml-2" data-controller="checkbox">
      <div class="d-flex flex-column">
        <h3 class="p-3 mb-3">PLANNING CHECKLIST</h3>
        <div class="item mt-5 mb-3">
          <input class="form-check-input" type="checkbox" value="" checked id="Create_your_event"> Create your event
        </div>
        <div class="item mb-3">
          <%= check_box_tag 'guest_list', current_user.guest_list, current_user.guest_list, data: {action: 'click->checkbox#changeValue'} %>
          <%= label_tag 'guest_list', 'Create your guest list' %>
        </div>
        <div class="item mb-3">
          <%= check_box_tag 'design_cards', current_user.design_cards, current_user.design_cards, data: {action: 'click->checkbox#changeValue'} %>
          <%= label_tag 'design_cards', 'Design your invitation card' %>
        </div>
        <div class="item mb-3">
          <%= check_box_tag 'send_cards', current_user.send_cards, current_user.send_cards, data: {action: 'click->checkbox#changeValue'} %>
          <%= label_tag 'send_cards', 'Send your invitation card' %>
        </div>
      </div>
    </div>
  </div>

  <div class="rsvp border rounded p-3 bg-white">
    <h3 class="p-3 mb-3">RSVP SUMMARY</h3>
    <div class="row row-cols-4 text-center">
      <p class="border-bottom"><%= @yes %></p>
      <p class="border-bottom"><%= @no %></p>
      <p class="border-bottom"><%= @awaiting %></p>
      <p class=""><%= @yes + @no + @awaiting%></p>
      <p>Yes</p>
      <p>No</p>
      <p>Awaiting</p>
      <p>Total</p>
    </div>

    <div class="d-flex justify-content-center mt-3">
      <%= link_to "Add your guests", event_guests_path(current_user.events.first), class: "btn" %>
    </div>
  </div>
</div>


<%# value="<%= current_user.guest_list ? "checked" : "unchecked" %>
